<!DOCTYPE html>
<html>
  <head>
    <title>Blocipedia - Marco Lopez-Silva's project</title>
    <%= csrf_meta_tags %>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <div class="container">
      <ul class="nav nav-tabs">
        <li class="pull-left"><%= link_to "Blocipedia Home", welcome_index_path %></li>
        <li class="pull-right">
            <% if user_signed_in? %>
                <p> Signed in as: <strong><%= current_user.email %></strong></p>
                <!-- Added next line to easily see the user's role -->
                <p> User role: <%= current_user.role.capitalize %>
                <p>
                    <%= link_to 'Edit Profile', edit_user_registration_path, :class => 'navbar-link text-info' %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link text-info' %>
                </p>
            <% else %>
                <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link'  %>
        </li>
        <% end %>
      </ul>

      <p class="notice text-success"><%= notice %></p-->
      <p class="alert text-danger"><%= alert %></p-->

      <%= yield %>
  </div>

  </body>

</html>
